<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/deep_green"
    android:orientation="vertical"
    tools:context=".Fragment.PreferencesFragment">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/vivid_orange"
        app:title="Preferences"
        android:paddingStart="16dp"
        app:titleTextColor="@color/white"
        app:contentInsetStart="0dp"
        app:contentInsetLeft="0dp"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:layout_scrollFlags="scroll|enterAlways" />

    <!-- Content Section -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="16dp">

        <!-- Content Section -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Switch to block mobile with a missed call -->
            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switchMissedCallLock"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Block Mobile with Missed Call"
                android:backgroundTint="@color/vivid_orange"
                android:textColor="@color/white"
                android:textStyle="bold"
                app:trackTint="@color/white"
                app:thumbTint="@color/vivid_orange"/>


            <!-- Switch to take pictures while the screen is off -->
            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switchScreenOffPictures"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:backgroundTint="@color/vivid_orange"
                android:text="Take Pictures While Screen Is Off"
                android:layout_marginTop="8dp"
                android:textStyle="bold"
                app:trackTint="@color/white"
                app:thumbTint="@color/vivid_orange"/>


            <androidx.appcompat.widget.LinearLayoutCompat
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_weight="1">


                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Failed Login Attempts Threshold:  "
                    android:textStyle="bold"
                    android:textColor="@color/white"
                    android:textSize="16sp"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/editTextUsername" />
                <!-- EditText for failed login attempts threshold -->
                <androidx.appcompat.widget.AppCompatEditText
                    android:id="@+id/editTextFailedAttempts"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textAlignment="viewEnd"
                    android:hint="3"
                    android:backgroundTint="@color/transparent"
                    android:textColor="@color/vivid_orange"
                    android:textColorHint="@color/vivid_orange"
                    android:inputType="number" />

            </androidx.appcompat.widget.LinearLayoutCompat>

            <!-- Save Preferences button -->
            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/buttonSavePreferences"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/button_background"
                android:text="Save Preferences"
                android:textColor="@color/white"
                android:layout_marginTop="16dp"/>

        </LinearLayout>
    </ScrollView>

</androidx.appcompat.widget.LinearLayoutCompat>
